@AGENTS.md

For deploy & VPS details (GitHub Actions pipeline, CloudPanel/pm2 runtime, nvm Node 24, `.env.local` handling, build-time env baking gotcha): see `.cursor/rules/deploy-github-actions.mdc` and `.cursor/rules/vps-cloudpanel-runtime.mdc`.
